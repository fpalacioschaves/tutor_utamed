{
  "name": "tutor-utamed",
  "private": true,
  "version": "0.16.0",
  "workspaces": [
    "api",
    "web"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "setup": "npm run db:setup -w api",
    "dev": "concurrently -n API,WEB \"npm run dev -w api\" \"npm run dev -w web\"",
    "dev:api": "npm run dev -w api",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w api && npm run build -w web",
    "typecheck": "npm run typecheck -w api && npm run typecheck -w web",
    "db:reset": "npm run db:reset -w api",
    "db:update": "npm run db:update -w api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
